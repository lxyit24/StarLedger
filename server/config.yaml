server:
  port: 8080
  mode: debug  # debug / release

database:
  driver: sqlite3
  dbname: starledger.db

jwt:
  secret: "starledger-jwt-secret-key-2026"
  expire: 24  # hours

log:
  level: debug  # debug / info / warn / error
  output: stdout  # stdout / file
  file_path: "./logs/app.log"
